name: Apply ZZI4 idempotent pre-stage

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/apply-zzi4-idempotent-prestage.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Apply ZZI4 idempotent pre-stage patch
        run: python3 tools/apply-zzi4-idempotent-prestage.py

      - name: Fix structural assertion literal
        run: |
          python3 - <<'PY'
          from pathlib import Path
          path = Path('app/src/test/java/dev/busung/s25uroot/Zzi4PostRootRuntimeTest.kt')
          text = path.read_text(encoding='utf-8')
          old = '        assertTrue(manual.contains("action=${if (reusedExisting) \\"reuse\\" else \\"refresh\\"}"))\n'
          new = (
              '        assertTrue(manual.contains("reusedExisting"))\n'
              '        assertTrue(manual.contains("action="))\n'
          )
          if old not in text:
              raise SystemExit('generated structural assertion anchor not found')
          path.write_text(text.replace(old, new, 1), encoding='utf-8')
          PY

      - name: Java 21
        uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: "21"

      - name: Gradle cache
        uses: gradle/actions/setup-gradle@v6

      - name: Test, lint and assemble release
        run: |
          set -euo pipefail
          chmod +x gradlew
          ./gradlew --build-cache \
            :app:testDebugUnitTest \
            :app:lintDebug \
            :app:assembleRelease

      - name: Commit validated ZZI4 pre-stage fix
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add \
            app/src/main/java/dev/busung/s25uroot/InstallViewModel.kt \
            app/src/main/java/dev/busung/s25uroot/AutoRootRunner.kt \
            app/src/test/java/dev/busung/s25uroot/Zzi4PostRootRuntimeTest.kt
          git diff --cached --check
          git commit -m 'Reuse verified ZZI4 KernelSU pre-stage before exploit'
          git push
